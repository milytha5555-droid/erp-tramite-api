services:
  - type: web
    name: erp-tramite-api
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_FILE
        value: /data/tramites.json
    disk:
      name: erp-tramite-data
      mountPath: /data
      sizeGB: 1
